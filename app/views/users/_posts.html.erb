<ul class="list-unstyled d-flex justify-content-around flex-wrap">
  <% posts.each do |post| %>
    <a href="/posts/<%= post.id %>">
      <li class="mt-3"><h5 class="text-dark m-0"><strong><%= post.title %></strong></h5></li>
      <li class="mb-2"><img src="<%= post.image %>" width="300"></li>
    </a>
  <% end %>
</ul>

<%= paginate posts %>

